# Proposed Phase 8 Plan

## Suggested scope

1. Add a versioned run registry that compares current and previous fingerprints and reports stale/changed packages.
2. Add JSON Schema files for context, manifest and validation reports, then validate them in tests.
3. Add fixture-based tests that do not depend on current VNSTOCK freshness.
4. Investigate, only with separate approval, safe sources for corporate actions, financial filing availability and canonical news entity mapping.
5. Keep all outputs descriptive and prohibit investment recommendations.

## Approval required

Phase 8 requires separate approval, especially for any new source, external tool, schema migration or changes to output-version policy.

## Provenance / Source Basis

This proposal follows the remaining limitations observed in Phase 7.

## Known Limitations

No Phase 8 implementation has started. New-source feasibility and schemas are not fully confirmed.

## How AI Should Use This

Use this document only as a proposal. Wait for explicit approval before performing Phase 8.
