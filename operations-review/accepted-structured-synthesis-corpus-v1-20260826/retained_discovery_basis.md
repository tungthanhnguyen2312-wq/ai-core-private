# Retained Accepted-Synthesis Discovery Basis

Consumer retained artifacts include the deterministic packet-parity universe and test fixtures, but no persisted accepted structured-synthesis response plus corresponding validated ticker context suitable for authoritative registration. Untracked evaluation artifacts were deliberately excluded.

`REAL_RETAINED_ACCEPTED_SYNTHESIS_COUNT = 0`. The empty corpus is therefore the valid real retained product. Registration behavior is exercised only with deterministic test fixtures and always reuses the existing Consumer acceptance boundary.
